# Changelog

## 0.6.1 — 2026-09-13

### 变更
- 移除网址 favicon 在线抓取——全应用唯一的联网点,Lulen 成为完全离线工具;
  `socket/select/ssl` 等网络模块与 `Qt6Network`、OpenSSL DLL 全部不再打包
- 单实例唤起从 QLocalServer/QLocalSocket(依赖 QtNetwork)改为 Win32 命名互斥体 + 命名事件;
  内核对象随进程退出自动销毁,不再有崩溃残留需要清理
- 网址条目图标:已缓存到本地的网站图标仍照常显示,新网址显示手绘地球占位,可自定义图标;
  命令条搜索词编码改为内置实现(不再引入 urllib.parse)

### 修复
- 开启「智能应用控制」(Win11 智能应用控制 / WDAC)的机器上双击 exe 启动即崩,报
  “DLL load failed while importing select: 应用程序控制策略已阻止此文件”——
  启动路径不再加载任何网络 DLL,该拦截无从发生
- 鼠标从边缘移入面板内部后 resize 光标不恢复,须移出窗口才复位——边缘光标改由
  Win32 WM_SETCURSOR 即时判定:系统在每次鼠标移动、显示光标前都会重问形状,
  一出热区立即换回鼠标下控件的实际光标(箭头/文本光标等),不再把光标常驻设到面板控件上

## 0.6.0 — 2026-09-05

### 变更
- 锁定面板现在意味着常驻——位置锁死、失焦不隐藏、启动条目后也不隐藏,并保持窗口置顶
- exe 版默认数据目录改为 exe 同目录的 `Lulen-Data\` 子文件夹(便携式,随 exe 走,不可写不兜底);
  源码运行仍用 `%APPDATA%\Lulen`;旧位置存在数据时首次启动自动迁移(含指针文件);`LULEN_HOME` 优先级不变
- 面板高度自适应的收缩下限改为约列数的 2/3,空面板/少量条目时不再被压成长条;新配置默认网格 7×5
- 应用图标重绘:对角渐变圆角方块 + 白色粗体 L,512px 绘制,ico 含 16~256 七档尺寸

### 修复
- 开机自启失效——exe 被移动后注册表仍指向旧路径且无提示。现在启动时自动校验并以当前 exe 为准
  重写注册表项(自愈),设置/托盘写入失败时弹气泡提示;`LULEN_HOME` 测试模式不触碰注册表
- 拖入 .lnk 快捷方式时存的是快捷方式自身路径,删除桌面图标后条目失效。现在拖入瞬间经 IShellLinkW
  解析出真实目标(含参数/起始位置),条目存目标路径,快捷方式删不删都不影响;解析不出有效目标(如 UWP
  快捷方式)仍存 .lnk 原路径

## 0.5.1 — 2026-08-31

- 新增:数据目录可自定义——设置 → 系统 → 数据目录,更改后自动迁移配置/备份/图标缓存并即时生效;
  自定义位置记录于指针文件 `%APPDATA%\Lulen\data_dir.txt`,删除即回默认位置;`LULEN_HOME` 环境变量优先级仍最高
- 清理:ruff 静态检查全量归零(规则固化在 pyproject.toml),杂项代码整理

## 0.5.0 — 2026-08-30

### 新增
- 组内拖动排序重做:目标格左/右半边决定插前/插后,拖动时绘制强调色指示线
- 跨组拖拽第二手势:悬停页签自动翻组后,把条目放进面板任意位置松手即移入
- 页签拖动排序(QTabBar 原生)、页签右键重命名/删除、"+" 紧跟最后一个分组名
- 边缘拖拽调大小:贴边出现缩放光标,松手自动回写列数/行数;手动调过后高度不再自动收缩
- 条目快捷键、命令条拼音前缀过滤、`↓` 进入结果列表
- 页签条占满可用宽度,分组名不再过早省略;"+" 紧跟最后一个分组名
- PyInstaller 单文件打包(`Lulen.spec`),含 conda 环境运行库收编与应用图标

### 修复
- 跨组拖拽:悬停翻组后落下面板被误判为组内排序而无操作(按条目 ID 反查源分组)
- 页签缝隙/边缘落下被静默忽略(吸附最近页签)
- 失焦自动隐藏与拖拽文件冲突(1.2s 宽限期 + 鼠标按住/拖拽悬停守卫)
- 边缘缩放热区参照错误(以可见面板为基准)与 150% 缩放下热区过窄
- 冻结模式(exe)下的开机自启路径
- 工具提示缺失、对话框期间误触发失焦隐藏、二次启动唤起面板

### 整理
- ruff 全量检查归零,规则固化在 pyproject.toml
- 文档全面重写;新增 CHANGELOG 与 MIT LICENSE

## 0.1.0 — 2026-08-29

- 首个可用版本:悬浮面板、条目(五类)、分组多页、设置窗口、深浅主题、
  托盘、全局热键、失焦隐藏、开机自启、单实例、配置原子写入与备份
